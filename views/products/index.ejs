<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Products</title>
</head>

<body>
    <% if (category) {%>
        <h1><%= capitalizeFirstLetter(category)%> Products:</h1>
    <%} else {%> 
        <h1>All Products:</h1>
    <% } %> 
    <ul>
        <% for(product of products){%>
                <li><a href="/products/<%=product._id%>"><%= product.name %></a></li>
        <%} %>
    </ul>
    <button><a style="text-decoration: none; color: inherit;" href="/products/new">New</a></button>
   
   
    <% if (category) {%>
    <form id="resetFilterForm" action="/products" method="GET" style="display: inline;">
        <button type="submit">Reset Filter</button>
    </form>
    <% } %>
   

</body>

</html>